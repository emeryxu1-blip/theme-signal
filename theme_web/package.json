{
  "name": "theme-signal-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test",
    "test": "node --experimental-strip-types tests/normalize.test.ts && node --experimental-strip-types tests/ingest.test.ts && node --experimental-strip-types tests/seo.test.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@cloudflare/workers-types": "^5.20260804.1",
    "@playwright/test": "^1.62.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.121.0"
  }
}
